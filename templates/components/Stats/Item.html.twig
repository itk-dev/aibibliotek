{% props label, value %}
<div class="flex flex-col gap-1 rounded-lg bg-surface-2 px-4 py-3">
    <dt class="whitespace-nowrap text-xs uppercase tracking-widest text-text-muted">{{ label|trans }}</dt>
    <dd class="text-3xl font-semibold text-ink">{{ value }}</dd>
</div>
