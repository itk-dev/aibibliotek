{% props kommune, model, name, summary, href = '#', class = '' %}
{# basis-[260px] grow: preferred width 260, expand to fill row slack. #}
{# max-w-[400px] keeps a lonely card on a wide row from stretching out. #}
<a class="group flex grow basis-[260px] max-w-[400px] flex-col gap-2 rounded-xl border border-line bg-surface p-5 no-underline shadow-sm transition-transform hover:-translate-y-0.5 hover:border-border-strong hover:shadow-md{{ class ? ' ' ~ class : '' }}"
   href="{{ href }}"
   role="listitem">
    <span class="text-xs uppercase tracking-widest text-text-muted">{{ kommune }} · {{ model }}</span>
    {# tracking-normal cancels the -0.01em letter-spacing that the #}
    {# @layer base rule applies to every heading, so the visual matches #}
    {# the previous <span>. #}
    <twig:Heading level="3" size="xs" class="tracking-normal group-hover:text-primary">{{ name }}</twig:Heading>
    <span class="text-sm leading-relaxed text-text">{{ summary }}</span>
</a>
