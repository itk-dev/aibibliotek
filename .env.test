# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

DATABASE_URL="mysql://root:password@mariadb:3306/db_test?serverVersion=10.11.16-MariaDB&charset=utf8mb4"

# Allow `*@example.test` through self-signup so the registration tests don't
# need to stand up their own service container.
REGISTRATION_ALLOWED_EMAIL_DOMAINS=example.test

MAILER_DSN=null://null
MAILER_FROM="AI Bibliotek <noreply@ai-lib.test>"

SUPPORTED_FRAMEWORKS="Open WebUI:openwebui"
